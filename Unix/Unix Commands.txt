/* 'cat' command  */
# cat /home/jay/filename
# cat file1 file2
# cat >file1
# cat file1.txt | more
# cat file1.txt | less
# cat -n file1.txt
# cat -e file1.txt
# cat -T file1.txt
# cat file1.txt; cat file2.txt; cat file3.txt
# cat file1 > file2
# cat file1 >> file2
# cat < file2
# cat file1 file2 file3 > file4
# cat file1 file2 file3 | sort > file4
# cd path/to/location
# cd ..
# cd .
# cd
# cd ~
# cd ~username
# cd -p path
# cd -n path
# cd -v path
# cd -
# cd -l path
# chgrp [option] group filesystemobject 
# chmod options permissions filename
# chmod u=rwx,g=rx,o=r filename
# chmod 754 filename
# chgrp -R groupname filenameorlist
# chgrp -v groupname filenameorlist
# chgrp -f groupname filenameorlist
# chgrp -c groupname filenameorlist 
# chgrp groupname filenameorlist --changes
# chgrp -f groupname filenameorlist 
# chgrp groupname filenameorlist --silent 
# chgrp groupname filenameorlist --quiet
# chgrp groupname filnameorlist --dereference
# chgrp -h groupname filenameorlist 
# chgrp groupname filenameorlist --no-dereference
# chgrp groupname filenameorlist --no-preserve-root
# chgrp groupname filenameorlist --preserve-root
# chgrp --reference=RFILE filenameorlist
# chgrp -L groupname filenameorlist
# chgrp -p groupname filenameorlist
